pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Building...'
            } // ✅ Pastikan kurung tutup berada di tempat yang benar
        }
    }
}
